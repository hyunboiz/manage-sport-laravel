<div class="col-md-4">
  <div class="card dashboard-card bg-{{ $bg }} text-white p-3 text-center">
    <i class="fa fa-{{ $icon }} dashboard-icon mb-2"></i>
    <div class="dashboard-title">{{ $title }}</div>
    <div class="dashboard-value">{{ $value }}</div>
  </div>
</div>